// Create gamePresets data up here
// var gamePresets = {};

// Example QUnit test
QUnit.test("hello test", function(assert) {
    assert.ok(1 == "1", "Passed!");
});

// Check that game was initialized correctly
QUnit.test("Test game data initialization", function(assert) {
    // var testGame = new Game(gamePresets);
    // assert.ok(testGame.gameData["houses"] === 81, "house preset correct");
    // assert.ok(testGame.gameData["hotels"] === 31, "hotel preset correct");
    // assert.ok(testGame.gameData["skyscrapers"] === 16, "skyscraper preset correct");
    // assert.ok(testGame.gameData["recentLocation"] === "go", "every player starts at GO");
    // assert.ok(observed value == expected value, "Comment on what the assertion checks");
});